<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Calculate Fuel CO2 Equivalent</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      margin: 0;
      padding: 0;
    }
    h1 {
      text-align: center;
      margin: 1rem 0;
    }
    form {
      max-width: 400px;
      margin: 0 auto;
      padding: 0 1rem;
    }
    label {
      font-weight: bold;
      margin-bottom: 0.5rem;
    }
    select, input[type="text"], button {
      width: 100%;
      padding: 0.5rem;
      margin-bottom: 1rem;
      border: 1px solid #ccc;
      border-radius: 5px;
      font-size: 1rem;
      box-sizing: border-box;
    }
    button {
      background-color: #333;
      color: white;
      border: none;
      cursor: pointer;
    }
    button:hover {
      background-color: #555;
    }
    h2 {
      margin-top: 1.5rem;
      font-size: 1.2rem;
      text-align: center;
    }
    p{
        text-align: center;
      font-size: 1rem;
    }
    
    /* Responsive styling */
    @media screen and (max-width: 600px) {
      form {
        max-width: 100%;
        padding: 0 0.5rem;
      }
      select, input[type="text"], button {
        padding: 0.4rem;
        font-size: 0.9rem;
      }
    }
  </style>
</head>
<body>
  <h1>Calculate Fuel CO2 Equivalent</h1>
  
  <form action="/calculate-fuel-co2e" method="POST">
    <label for="fuelType">Fuel Type: </label>
    <select name="fuelType" required>
      <option value="Petrol">Petrol</option>
      <option value="Diesel">Diesel</option>
      <option value="LPG">LPG</option>
      <!-- Add other fuel types here -->
    </select><br>
    
    <label for="litres">Litres: </label>
    <input type="text" name="litres" required><br>
    
    <button type="submit">Calculate Fuel CO2 Equivalent</button>
  </form>
  
  <% if (typeof fuelCO2eData !== 'undefined') { %>
    <h2>Fuel CO2 Equivalent Data:</h2>
    <p><%= fuelCO2eData.carbonEquivalent %></p>
  <% } %>
</body>
</html>
